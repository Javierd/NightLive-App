A working Google Maps API is needed at AndroidManifest.xml (the included one is disabled)
```xml
<meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="WORKING_GMAPS_ANDROID_API" />
```
